@model IEnumerable<DTO.Room>

@{
    ViewBag.Title = "List Rooms";
    Layout = "_AdminLayout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="wirdt=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/css/styles.css" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
</head>
<body>
    <h4>Here is the list of rooms</h4>

    <table class="table table-sm table-striped table-bordered">
        <thead>
            <tr>
                <th>Room Number</th>
                <th>Capacity</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var m in Model)
            {

                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => m.RoomNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => m.Capacity)
                    </td>
                    <td class="text-center">
                        <input type="button" class="btn btn-sm btn-warning" value="List Seats"
                               onclick="location.href='@Url.Action("AListSeats", new { id = m.Id })'" />
                        <input type="button" class="btn btn-danger btn-sm" value="Delete"
                               onclick="location.href='@Url.Action("ADeleteRoom", new { id = m.Id })'" />
                    </td>

                </tr>

            }
        </tbody>
    </table>
    <div class="text-left">
        <a asp-action="AMain" class="btn btn-primary">Back</a>
    </div>
</body>
</html>

